# general
math function

# Number Classification API

A RESTful API that accepts an integer via query parameters and returns its mathematical properties (prime, perfect, Armstrong, parity, digit sum) alongside a fun fact retrieved from the Numbers API.



## Specification

 **Endpoint:** 
  **Response Format:** JSON
 **CORS:** Enabled for cross-origin access

## Example Requests & Responses

### 1. Successful Response (`200 OK`)

**Request:**
http
